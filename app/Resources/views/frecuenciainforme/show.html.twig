{% extends 'base.html.twig' %}

{% block body %}
    <h1>Frecuenciainforme</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ frecuenciaInforme.id }}</td>
            </tr>
            <tr>
                <th>Frecuenciainforme</th>
                <td>{{ frecuenciaInforme.frecuenciaInforme }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('frecuenciainforme_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('frecuenciainforme_edit', { 'id': frecuenciaInforme.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
